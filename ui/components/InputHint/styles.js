import styled from 'styled-components';

const InputHint = styled.div`
  display: block;
  margin-top: 8px;
  font-style: italic;
  color: var(--gray-light);
  font-size: 13px;
`;

export default {
  InputHint,
};
